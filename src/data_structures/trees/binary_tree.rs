pub struct BinaryTree {}

impl BinaryTree {
    pub fn insert() {}
    pub fn find() {}
    pub fn remove() {
        // with no subtree

        // with one subtree

        // with two subtree
    }
}
